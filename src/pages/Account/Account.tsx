import React from "react";

import "./account.scss";

const Account: React.FC = () => {
    return (
        <section className="account">
            <div className="wrapper">Accout</div>
        </section>
    );
};

export default Account;
