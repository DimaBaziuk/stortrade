import React from "react";

import "./catalog.scss";

const Catalog: React.FC = () => {
    return (
        <section className="catalog">
            <div className="wrapper">Catalog</div>
        </section>
    );
};

export default Catalog;
